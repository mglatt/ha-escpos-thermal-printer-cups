print_text_utf8:
  name: Print Text (UTF-8)
  description: Print UTF-8 text with automatic transcoding to printer codepage. Select one or more printer devices as the target.
  fields:
    device_id:
      name: Device
      description: Select one or more printer devices (if omitted, broadcasts to all printers)
      required: false
      selector:
        device:
          integration: escpos_printer
    text:
      name: Text
      description: The text to print (UTF-8 characters will be converted to printer-compatible encoding)
      required: true
      example: "Hello, world! Café résumé naïve"
      selector:
        text:
          multiline: true
    align:
      name: Alignment
      description: Text alignment
      selector:
        select:
          options:
            - "left"
            - "center"
            - "right"
    bold:
      name: Bold
      description: Enable bold text
      selector:
        boolean: {}
    underline:
      name: Underline
      description: Underline style
      selector:
        select:
          options:
            - "none"
            - "single"
            - "double"
    width:
      name: Width
      description: Font width (normal/double/triple, or 1-8 in YAML)
      selector:
        select:
          options:
            - "normal"
            - "double"
            - "triple"
            - "1"
            - "2"
            - "3"
            - "4"
            - "5"
            - "6"
            - "7"
            - "8"
    height:
      name: Height
      description: Font height (normal/double/triple, or 1-8 in YAML)
      selector:
        select:
          options:
            - "normal"
            - "double"
            - "triple"
            - "1"
            - "2"
            - "3"
            - "4"
            - "5"
            - "6"
            - "7"
            - "8"
    cut:
      name: Cut
      description: Paper cutting mode after printing
      selector:
        select:
          options:
            - "none"
            - "partial"
            - "full"
    feed:
      name: Feed Lines
      description: Number of lines to feed after printing
      selector:
        number:
          min: 0
          max: 10
          step: 1
          mode: slider

print_text:
  name: Print Text (Raw Encoding)
  description: Print text to the ESC/POS printer using raw encoding. Select one or more printer devices as the target.
  fields:
    device_id:
      name: Device
      description: Select one or more printer devices (if omitted, broadcasts to all printers)
      required: false
      selector:
        device:
          integration: escpos_printer
    text:
      name: Text
      description: The text to print
      required: true
      example: "Hello, world!"
      selector:
        text:
          multiline: true
    align:
      name: Alignment
      description: Text alignment
      selector:
        select:
          options:
            - "left"
            - "center"
            - "right"
    bold:
      name: Bold
      description: Enable bold text
      selector:
        boolean: {}
    underline:
      name: Underline
      description: Underline style
      selector:
        select:
          options:
            - "none"
            - "single"
            - "double"
    width:
      name: Width
      description: Font width (normal/double/triple, or 1-8 in YAML)
      selector:
        select:
          options:
            - "normal"
            - "double"
            - "triple"
            - "1"
            - "2"
            - "3"
            - "4"
            - "5"
            - "6"
            - "7"
            - "8"
    height:
      name: Height
      description: Font height (normal/double/triple, or 1-8 in YAML)
      selector:
        select:
          options:
            - "normal"
            - "double"
            - "triple"
            - "1"
            - "2"
            - "3"
            - "4"
            - "5"
            - "6"
            - "7"
            - "8"
    encoding:
      name: Encoding
      description: Character encoding override
      selector:
        text: {}
    cut:
      name: Cut
      description: Paper cutting mode after printing
      selector:
        select:
          options:
            - "none"
            - "partial"
            - "full"
    feed:
      name: Feed Lines
      description: Number of lines to feed after printing
      selector:
        number:
          min: 0
          max: 10
          step: 1
          mode: slider

print_qr:
  name: Print QR Code
  description: Print a QR code. Select one or more printer devices as the target.
  fields:
    device_id:
      name: Device
      description: Select one or more printer devices (if omitted, broadcasts to all printers)
      required: false
      selector:
        device:
          integration: escpos_printer
    data:
      name: Data
      description: Data to encode in the QR code
      required: true
      example: "https://example.com"
      selector:
        text: {}
    size:
      name: Size
      description: QR code size (1-16)
      selector:
        number:
          min: 1
          max: 16
          step: 1
          mode: slider
    ec:
      name: Error Correction
      description: Error correction level
      selector:
        select:
          options:
            - "L"
            - "M"
            - "Q"
            - "H"
    align:
      name: Alignment
      description: QR code alignment
      selector:
        select:
          options:
            - "left"
            - "center"
            - "right"
    cut:
      name: Cut
      description: Paper cutting mode after printing
      selector:
        select:
          options:
            - "none"
            - "partial"
            - "full"
    feed:
      name: Feed Lines
      description: Number of lines to feed after printing
      selector:
        number:
          min: 0
          max: 10
          step: 1
          mode: slider

print_image:
  name: Print Image
  description: Print an image by URL or local path. Select one or more printer devices as the target.
  fields:
    device_id:
      name: Device
      description: Select one or more printer devices (if omitted, broadcasts to all printers)
      required: false
      selector:
        device:
          integration: escpos_printer
    image:
      name: Image
      description: Image URL or local file path
      required: true
      example: "/config/www/logo.png"
      selector:
        text: {}
    high_density:
      name: High Density
      description: Use high-density printing mode
      selector:
        boolean: {}
    impl:
      name: Implementation
      description: ESC/POS image command to use (overrides the printer's configured default)
      required: false
      selector:
        select:
          options:
            - "bitImageRaster"
            - "bitImageColumn"
            - "graphics"
    align:
      name: Alignment
      description: Image alignment
      selector:
        select:
          options:
            - "left"
            - "center"
            - "right"
    cut:
      name: Cut
      description: Paper cutting mode after printing
      selector:
        select:
          options:
            - "none"
            - "partial"
            - "full"
    feed:
      name: Feed Lines
      description: Number of lines to feed after printing
      selector:
        number:
          min: 0
          max: 10
          step: 1
          mode: slider

print_barcode:
  name: Print Barcode
  description: Print a barcode. Select one or more printer devices as the target.
  fields:
    device_id:
      name: Device
      description: Select one or more printer devices (if omitted, broadcasts to all printers)
      required: false
      selector:
        device:
          integration: escpos_printer
    code:
      name: Code
      description: Barcode data
      required: true
      example: "4006381333931"
      selector:
        text: {}
    bc:
      name: Barcode Type
      description: Type of barcode to print
      required: true
      example: "EAN13"
      selector:
        select:
          options:
            - "EAN13"
            - "EAN8"
            - "JAN13"
            - "JAN8"
            - "UPC-A"
            - "UPC-E"
            - "CODE39"
            - "CODE93"
            - "CODE128"
            - "ITF"
            - "ITF14"
            - "CODABAR"
    height:
      name: Height
      description: Barcode height in dots
      selector:
        number:
          min: 1
          max: 255
          step: 1
          mode: slider
    width:
      name: Width
      description: Barcode width (2-6)
      selector:
        number:
          min: 2
          max: 6
          step: 1
          mode: slider
    pos:
      name: Text Position
      description: Position of human-readable text
      selector:
        select:
          options:
            - "ABOVE"
            - "BELOW"
            - "BOTH"
            - "OFF"
    font:
      name: Font
      description: Font for human-readable text
      selector:
        select:
          options:
            - "A"
            - "B"
    align_ct:
      name: Center Align
      description: Center align the barcode
      selector:
        boolean: {}
    check:
      name: Checksum
      description: Enable checksum validation
      selector:
        boolean: {}
    force_software:
      name: Rendering Mode
      description: Force software rendering mode
      selector:
        select:
          options:
            - "true"
            - "false"
            - "graphics"
            - "bitImageColumn"
            - "bitImageRaster"
    align:
      name: Alignment
      description: Barcode alignment
      selector:
        select:
          options:
            - "left"
            - "center"
            - "right"
    cut:
      name: Cut
      description: Paper cutting mode after printing
      selector:
        select:
          options:
            - "none"
            - "partial"
            - "full"
    feed:
      name: Feed Lines
      description: Number of lines to feed after printing
      selector:
        number:
          min: 0
          max: 10
          step: 1
          mode: slider

feed:
  name: Feed Paper
  description: Feed the paper a number of lines. Select one or more printer devices as the target.
  fields:
    device_id:
      name: Device
      description: Select one or more printer devices (if omitted, broadcasts to all printers)
      required: false
      selector:
        device:
          integration: escpos_printer
    lines:
      name: Lines
      description: Number of lines to feed
      required: true
      selector:
        number:
          min: 1
          max: 10
          step: 1
          mode: slider

cut:
  name: Cut Paper
  description: Cut the paper. Select one or more printer devices as the target.
  fields:
    device_id:
      name: Device
      description: Select one or more printer devices (if omitted, broadcasts to all printers)
      required: false
      selector:
        device:
          integration: escpos_printer
    mode:
      name: Mode
      description: Cut type (full or partial)
      required: true
      selector:
        select:
          options:
            - "full"
            - "partial"

beep:
  name: Beep
  description: Trigger the printer buzzer (if supported). Select one or more printer devices as the target.
  fields:
    device_id:
      name: Device
      description: Select one or more printer devices (if omitted, broadcasts to all printers)
      required: false
      selector:
        device:
          integration: escpos_printer
    times:
      name: Times
      description: Number of beeps
      selector:
        number:
          min: 1
          max: 10
          step: 1
          mode: slider
    duration:
      name: Duration
      description: Duration of each beep
      selector:
        number:
          min: 1
          max: 10
          step: 1
          mode: slider

print_message:
  name: Print Formatted Message
  description: Print a message with full text formatting (bold, underline, size, alignment). Targets an ESC/POS notify entity.
  target:
    entity:
      domain: notify
      integration: escpos_printer
  fields:
    message:
      name: Message
      description: The text to print
      required: true
      example: "Hello, world!"
      selector:
        text:
          multiline: true
    title:
      name: Title
      description: Optional title printed before the message
      required: false
      selector:
        text: {}
    align:
      name: Alignment
      description: Text alignment
      selector:
        select:
          options:
            - "left"
            - "center"
            - "right"
    bold:
      name: Bold
      description: Enable bold text
      selector:
        boolean: {}
    underline:
      name: Underline
      description: Underline style
      selector:
        select:
          options:
            - "none"
            - "single"
            - "double"
    width:
      name: Width
      description: Font width (normal/double/triple, or 1-8 in YAML)
      selector:
        select:
          options:
            - "normal"
            - "double"
            - "triple"
            - "1"
            - "2"
            - "3"
            - "4"
            - "5"
            - "6"
            - "7"
            - "8"
    height:
      name: Height
      description: Font height (normal/double/triple, or 1-8 in YAML)
      selector:
        select:
          options:
            - "normal"
            - "double"
            - "triple"
            - "1"
            - "2"
            - "3"
            - "4"
            - "5"
            - "6"
            - "7"
            - "8"
    utf8:
      name: UTF-8 Mode
      description: Enable UTF-8 transcoding (converts special characters to printer-compatible encoding)
      selector:
        boolean: {}
    encoding:
      name: Encoding
      description: Character encoding override (ignored when UTF-8 mode is enabled)
      selector:
        text: {}
    cut:
      name: Cut
      description: Paper cutting mode after printing
      selector:
        select:
          options:
            - "none"
            - "partial"
            - "full"
    feed:
      name: Feed Lines
      description: Number of lines to feed after printing
      selector:
        number:
          min: 0
          max: 10
          step: 1
          mode: slider
